# team1

There is only a shell frontend currently.

To run the backend server from the root directory:

1. cd backend
2. source venv/bin/activate (this is the mac or linux command)
3. python app.py

Once the server is running open your browser to 127.0.0.1:5000 

